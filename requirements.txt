# BioNetFlux - Production Requirements
# Install with: pip install -e .
# Or use this file directly: pip install -r requirements.txt
#
# For development: pip install -e ".[dev]"
# For extras (pandas, toml): pip install -e ".[extras]"

numpy>=1.24.0
matplotlib>=3.6.0
sympy>=1.12
