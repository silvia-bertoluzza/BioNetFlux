    # MMS test: full_test_lin_phi
    # Auto-generated by validation_scripts/mms_ooc_upwind.py
    #
    # Manufactured solutions:
    #   u     = sin(s)*cos(t) + 1.0
    #   omega = 0.01*s**2*t + 0.5
    #   v     = t*sin(s + 2*t) + 1.0
    #   phi   = s*t
    #
    # Forcing terms:
    #   f_u     = (-15.0*t**2*(sin(s)*cos(t) + 1) + 15.0*t*(0.5*s*t + 1)*cos(s)*cos(t) + (0.5*s*t + 1)**3*(-sin(t) + 3.0*cos(t))*sin(s))/(0.5*s*t + 1)**3
    #   f_omega = 0.01*s**2*t + 0.01*s**2 - 0.02*t - 1.0*sin(s)*cos(t) - 0.5
    #   f_v     = 1.0*t*sin(s + 2*t) + 2*t*cos(s + 2*t) + sin(s + 2*t)
    #   f_phi   = 1.0*s*t + 1.0*s - 1.0*t*sin(s + 2*t) - 1.0

    [problem]
    name = "MMS_full_test_lin_phi"
    neq = 4
    problem_type = "ooc"

    [time_parameters]
    T  = 1.0
    dt = 0.025

    [physical_parameters.viscosity]
    nu      = 3.0
    mu      = 1.0
    epsilon = 1.0
    sigma   = 1.0

    [physical_parameters.reaction]
    a = 1.0
    c = 1.0

    [physical_parameters.coupling]
    b = 1.0
    d = 1.0

    [physical_parameters.chemotaxis]
    type = "receptor_saturation"
    k1   = 20.0
    k2   = 2.0

    [physical_parameters.tumor_suppression]
    m1 = 0.0
    m2 = 1.0

    [discretization]
    n_elements = 40
    tau = [0.5, 0.5, 0.5, 0.5]
    gam = 0.0   # Upwinding parameter for static condensation (0.0 = no upwind)

    [initial_conditions]
    u     = "sin(s) + 1.0"
    omega = "0.500000000000000"
    v     = "1.00000000000000"
    phi   = "zeros"

    [force_functions]
    u     = "(-15.0*t**2*(sin(s)*cos(t) + 1) + 15.0*t*(0.5*s*t + 1)*cos(s)*cos(t) + (0.5*s*t + 1)**3*(-sin(t) + 3.0*cos(t))*sin(s))/(0.5*s*t + 1)**3"
    omega = "0.01*s**2*t + 0.01*s**2 - 0.02*t - 1.0*sin(s)*cos(t) - 0.5"
    v     = "1.0*t*sin(s + 2*t) + 2*t*cos(s + 2*t) + sin(s + 2*t)"
    phi   = "1.0*s*t + 1.0*s - 1.0*t*sin(s + 2*t) - 1.0"

    [exact_solutions]
    u     = "sin(s)*cos(t) + 1.0"
    omega = "0.01*s**2*t + 0.5"
    v     = "t*sin(s + 2*t) + 1.0"
    phi   = "s*t"

    [exact_solution_derivatives]
    u     = "cos(s)*cos(t)"
    omega = "0.02*s*t"
    v     = "t*cos(s + 2*t)"
    phi   = "t"

    [boundary_conditions]
    # Dirichlet at B0 (left, s=0), Neumann at B1 (right).
    # Neumann data = outward normal flux = +flux at right.
    B0_u = { type = "dirichlet", data = "sin(s)*cos(t) + 1.0" }
B1_u = { type = "neumann",   data = "(-15.0*t*(sin(s)*cos(t) + 1.0) + 3.0*(0.5*s*t + 1)**2*cos(s)*cos(t))/(0.5*s*t + 1)**2" }
B0_omega = { type = "dirichlet", data = "0.01*s**2*t + 0.5" }
B1_omega = { type = "neumann",   data = "0.02*s*t" }
B0_v = { type = "dirichlet", data = "t*sin(s + 2*t) + 1.0" }
B1_v = { type = "neumann",   data = "1.0*t*cos(s + 2*t)" }
B0_phi = { type = "dirichlet", data = "s*t" }
B1_phi = { type = "neumann",   data = "1.0*t" }
