    # MMS test: chemo_satur_sin
    # Auto-generated by validation_scripts/mms_ooc_upwind.py
    #
    # Manufactured solutions:
    #   u     = t*sin(s)
    #   omega = 0
    #   v     = 0
    #   phi   = t*sin(s)
    #
    # Forcing terms:
    #   f_u     = (-0.25*t**3*sin(s)*cos(s)**2 + 0.25*t**2*(0.5*t*sin(s) + 1.0)*cos(2*s) + (1.0*t + 1)*(0.5*t*sin(s) + 1.0)**3*sin(s))/(0.5*t*sin(s) + 1.0)**3
    #   f_omega = 0
    #   f_v     = 0
    #   f_phi   = (2.0*t + 1)*sin(s)

    [problem]
    name = "MMS_chemo_satur_sin"
    neq = 4
    problem_type = "ooc"

    [time_parameters]
    T  = 1.0
    dt = 0.025

    [physical_parameters.viscosity]
    nu      = 1.0
    mu      = 1.0
    epsilon = 1.0
    sigma   = 1.0

    [physical_parameters.reaction]
    a = 1.0
    c = 1.0

    [physical_parameters.coupling]
    b = 0.0
    d = 0.0

    [physical_parameters.chemotaxis]
    type = "receptor_saturation"
    k1   = 1.0
    k2   = 2.0

    [physical_parameters.tumor_suppression]
    m1 = 0.0
    m2 = 1.0

    [discretization]
    n_elements = 40
    tau = [0.5, 0.5, 0.5, 0.5]
    gam = 1.0   # Upwinding parameter for static condensation (0.0 = no upwind)

    [initial_conditions]
    u     = "zeros"
    omega = "zeros"
    v     = "zeros"
    phi   = "zeros"

    [force_functions]
    u     = "(-0.25*t**3*sin(s)*cos(s)**2 + 0.25*t**2*(0.5*t*sin(s) + 1.0)*cos(2*s) + (1.0*t + 1)*(0.5*t*sin(s) + 1.0)**3*sin(s))/(0.5*t*sin(s) + 1.0)**3"
    omega = "zeros"
    v     = "zeros"
    phi   = "(2.0*t + 1)*sin(s)"

    [exact_solutions]
    u     = "t*sin(s)"
    omega = "zeros"
    v     = "zeros"
    phi   = "t*sin(s)"

    [exact_solution_derivatives]
    u     = "t*cos(s)"
    omega = "zeros"
    v     = "zeros"
    phi   = "t*cos(s)"

    [boundary_conditions]
    # Dirichlet at B0 (left, s=0), Neumann at B1 (right).
    # Neumann data = outward normal flux = +flux at right.
    B0_u = { type = "dirichlet", data = "t*sin(s)" }
B1_u = { type = "neumann",   data = "t*(0.25*t**2*sin(s)**2 + 0.75*t*sin(s) + 1.0)*cos(s)/(0.5*t*sin(s) + 1.0)**2" }
# omega: exact solution zero — homogeneous Neumann by default
# v: exact solution zero — homogeneous Neumann by default
B0_phi = { type = "dirichlet", data = "t*sin(s)" }
B1_phi = { type = "neumann",   data = "1.0*t*cos(s)" }
