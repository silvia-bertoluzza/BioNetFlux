    # MMS test: fully_coupled
    # Auto-generated by validation_scripts/mms_ooc_upwind.py
    #
    # Manufactured solutions:
    #   u     = t*sin(s)
    #   omega = sin(s + t)
    #   v     = t*sin(s)
    #   phi   = t**2*sin(s + 2*t)
    #
    # Forcing terms:
    #   f_u     = -1.0*t**3*sin(s)*sin(s + 2*t) + 1.0*t**3*cos(s)*cos(s + 2*t) + 1.0*t*sin(s) + 1.0*sin(s)
    #   f_omega = -1.0*t*sin(s) + 2.0*sin(s + t) + cos(s + t)
    #   f_v     = (1.0*t + 1)*sin(s)
    #   f_phi   = t*(2.0*t*sin(s + 2*t) + 2*t*cos(s + 2*t) - 1.0*sin(s) + 2*sin(s + 2*t))

    [problem]
    name = "MMS_fully_coupled"
    neq = 4
    problem_type = "ooc"

    [time_parameters]
    T  = 1.0
    dt = 0.025

    [physical_parameters.viscosity]
    nu      = 1.0
    mu      = 1.0
    epsilon = 1.0
    sigma   = 1.0

    [physical_parameters.reaction]
    a = 1.0
    c = 1.0

    [physical_parameters.coupling]
    b = 1.0
    d = 1.0

    [physical_parameters.chemotaxis]
    # type = "constant"
    type = "constant"
    k1   = 0.0
    k2   = 1.0

    [physical_parameters.tumor_suppression]
    m1 = 0.0
    m2 = 1.0

    [discretization]
    n_elements = 40
    tau = [0.5, 0.5, 0.5, 0.5]

    [initial_conditions]
    u     = "zeros"
    omega = "sin(s)"
    v     = "zeros"
    phi   = "zeros"

    [force_functions]
    u     = "-1.0*t**3*sin(s)*sin(s + 2*t) + 1.0*t**3*cos(s)*cos(s + 2*t) + 1.0*t*sin(s) + 1.0*sin(s)"
    omega = "-1.0*t*sin(s) + 2.0*sin(s + t) + cos(s + t)"
    v     = "(1.0*t + 1)*sin(s)"
    phi   = "t*(2.0*t*sin(s + 2*t) + 2*t*cos(s + 2*t) - 1.0*sin(s) + 2*sin(s + 2*t))"

    [exact_solutions]
    u     = "t*sin(s)"
    omega = "sin(s + t)"
    v     = "t*sin(s)"
    phi   = "t**2*sin(s + 2*t)"

    [exact_solution_derivatives]
    u     = "t*cos(s)"
    omega = "cos(s + t)"
    v     = "t*cos(s)"
    phi   = "t**2*cos(s + 2*t)"

    [boundary_conditions]
    # Dirichlet at B0 (left, s=0), Neumann at B1 (right).
    # Neumann data = outward normal flux = +flux at right.
    B0_u = { type = "dirichlet", data = "t*sin(s)" }
B1_u = { type = "neumann",   data = "1.0*t*(-t**2*sin(s)*cos(s + 2*t) + cos(s))" }
B0_omega = { type = "dirichlet", data = "sin(s + t)" }
B1_omega = { type = "neumann",   data = "1.0*cos(s + t)" }
B0_v = { type = "dirichlet", data = "t*sin(s)" }
B1_v = { type = "neumann",   data = "1.0*t*cos(s)" }
B0_phi = { type = "dirichlet", data = "t**2*sin(s + 2*t)" }
B1_phi = { type = "neumann",   data = "1.0*t**2*cos(s + 2*t)" }
