    # MMS test: full_test_linear
    # Auto-generated by validation_scripts/mms_ooc_upwind.py
    #
    # Manufactured solutions:
    #   u     = 0.5*s*t + 0.5
    #   omega = 0
    #   v     = 0
    #   phi   = -s*t
    #
    # Forcing terms:
    #   f_u     = 1.5*s*t**3/(0.5*s*t - 1)**3 + 0.5*s + 6.0*t**2/(0.5*s*t - 1)**3
    #   f_omega = -0.5*s*t - 0.5
    #   f_v     = 0
    #   f_phi   = s*(-1.0*t - 1)

    [problem]
    name = "MMS_full_test_linear"
    neq = 4
    problem_type = "ooc"

    [time_parameters]
    T  = 1.0
    dt = 0.025

    [physical_parameters.viscosity]
    nu      = 3.0
    mu      = 1.0
    epsilon = 1.0
    sigma   = 1.0

    [physical_parameters.reaction]
    a = 1.0
    c = 1.0

    [physical_parameters.coupling]
    b = 1.0
    d = 1.0

    [physical_parameters.chemotaxis]
    type = "receptor_saturation"
    k1   = 8.0
    k2   = 2.0

    [physical_parameters.tumor_suppression]
    m1 = 0.0
    m2 = 1.0

    [discretization]
    n_elements = 40
    tau = [0.5, 0.5, 0.5, 0.5]
    gam = 1.0   # Upwinding parameter for static condensation (0.0 = no upwind)

    [initial_conditions]
    u     = "0.500000000000000"
    omega = "zeros"
    v     = "zeros"
    phi   = "zeros"

    [force_functions]
    u     = "1.5*s*t**3/(0.5*s*t - 1)**3 + 0.5*s + 6.0*t**2/(0.5*s*t - 1)**3"
    omega = "-0.5*s*t - 0.5"
    v     = "zeros"
    phi   = "s*(-1.0*t - 1)"

    [exact_solutions]
    u     = "0.5*s*t + 0.5"
    omega = "zeros"
    v     = "zeros"
    phi   = "-s*t"

    [exact_solution_derivatives]
    u     = "0.5*t"
    omega = "zeros"
    v     = "zeros"
    phi   = "-t"

    [boundary_conditions]
    # Dirichlet at B0 (left, s=0), Neumann at B1 (right).
    # Neumann data = outward normal flux = +flux at right.
    B0_u = { type = "dirichlet", data = "0.5*s*t + 0.5" }
B1_u = { type = "neumann",   data = "t*(3.0*s*t + 1.5*(0.5*s*t - 1)**2 + 3.0)/(0.5*s*t - 1)**2" }
# omega: exact solution zero — homogeneous Neumann by default
# v: exact solution zero — homogeneous Neumann by default
B0_phi = { type = "dirichlet", data = "-s*t" }
B1_phi = { type = "neumann",   data = "-1.0*t" }
