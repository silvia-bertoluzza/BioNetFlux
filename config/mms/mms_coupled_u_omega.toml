    # MMS test: coupled_u_omega
    # Auto-generated by validation_scripts/mms_ooc_upwind.py
    #
    # Manufactured solutions:
    #   u     = s*t
    #   omega = t*sin(s)
    #   v     = 0
    #   phi   = 0
    #
    # Forcing terms:
    #   f_u     = s
    #   f_omega = -1.0*s*t + 2.0*t*sin(s) + sin(s)
    #   f_v     = 0
    #   f_phi   = 0

    [problem]
    name = "MMS_coupled_u_omega"
    neq = 4
    problem_type = "ooc"

    [time_parameters]
    T  = 1.0
    dt = 0.025

    [physical_parameters.viscosity]
    nu      = 1.0
    mu      = 1.0
    epsilon = 1.0
    sigma   = 1.0

    [physical_parameters.reaction]
    a = 1.0
    c = 1.0

    [physical_parameters.coupling]
    b = 0.0
    d = 1.0

    [physical_parameters.chemotaxis]
    type = "constant"
    k1   = 0.0
    k2   = 1.0

    [physical_parameters.tumor_suppression]
    m1 = 0.0
    m2 = 1.0

    [discretization]
    n_elements = 40
    tau = [0.5, 0.5, 0.5, 0.5]

    [initial_conditions]
    u     = "zeros"
    omega = "zeros"
    v     = "zeros"
    phi   = "zeros"

    [force_functions]
    u     = "s"
    omega = "-1.0*s*t + 2.0*t*sin(s) + sin(s)"
    v     = "zeros"
    phi   = "zeros"

    [exact_solutions]
    u     = "s*t"
    omega = "t*sin(s)"
    v     = "zeros"
    phi   = "zeros"

    [exact_solution_derivatives]
    u     = "t"
    omega = "t*cos(s)"
    v     = "zeros"
    phi   = "zeros"

    [boundary_conditions]
    # Dirichlet at B0 (left, s=0), Neumann at B1 (right).
    # Neumann data = outward normal flux = +flux at right.
    B0_u = { type = "dirichlet", data = "s*t" }
B1_u = { type = "neumann",   data = "1.0*t" }
B0_omega = { type = "dirichlet", data = "t*sin(s)" }
B1_omega = { type = "neumann",   data = "1.0*t*cos(s)" }
# v: exact solution zero — homogeneous Neumann by default
# phi: exact solution zero — homogeneous Neumann by default
