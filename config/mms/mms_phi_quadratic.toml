    # MMS test: phi_quadratic
    # Auto-generated by validation_scripts/mms_ooc_upwind.py
    #
    # Manufactured solutions:
    #   u     = 0
    #   omega = 0
    #   v     = 0
    #   phi   = s**2*t
    #
    # Forcing terms:
    #   f_u     = 0
    #   f_omega = 0
    #   f_v     = 0
    #   f_phi   = 1.0*s**2*t + s**2 - 2.0*t

    [problem]
    name = "MMS_phi_quadratic"
    neq = 4
    problem_type = "ooc"

    [time_parameters]
    T  = 1.0
    dt = 0.025

    [physical_parameters.viscosity]
    nu      = 1.0
    mu      = 1.0
    epsilon = 1.0
    sigma   = 1.0

    [physical_parameters.reaction]
    a = 1.0
    c = 1.0

    [physical_parameters.coupling]
    b = 0.0
    d = 0.0

    [physical_parameters.chemotaxis]
    type = "constant"
    k1   = 0.0
    k2   = 1.0

    [physical_parameters.tumor_suppression]
    m1 = 0.0
    m2 = 1.0

    [discretization]
    n_elements = 40
    tau = [0.5, 0.5, 0.5, 0.5]

    [initial_conditions]
    u     = "zeros"
    omega = "zeros"
    v     = "zeros"
    phi   = "zeros"

    [force_functions]
    u     = "zeros"
    omega = "zeros"
    v     = "zeros"
    phi   = "1.0*s**2*t + s**2 - 2.0*t"

    [exact_solutions]
    u     = "zeros"
    omega = "zeros"
    v     = "zeros"
    phi   = "s**2*t"

    [exact_solution_derivatives]
    u     = "zeros"
    omega = "zeros"
    v     = "zeros"
    phi   = "2*s*t"

    [boundary_conditions]
    # Dirichlet at B0 (left, s=0), Neumann at B1 (right).
    # Neumann data = outward normal flux = +flux at right.
    # u: exact solution zero — homogeneous Neumann by default
# omega: exact solution zero — homogeneous Neumann by default
# v: exact solution zero — homogeneous Neumann by default
B0_phi = { type = "dirichlet", data = "s**2*t" }
B1_phi = { type = "neumann",   data = "2.0*s*t" }
