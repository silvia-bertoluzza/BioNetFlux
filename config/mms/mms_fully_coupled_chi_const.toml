    # MMS test: fully_coupled_chi_const
    # Auto-generated by validation_scripts/mms_ooc_upwind.py
    #
    # Manufactured solutions:
    #   u     = t*sin(s)
    #   omega = t*sin(s)
    #   v     = t*sin(s)
    #   phi   = t*sin(s)
    #
    # Forcing terms:
    #   f_u     = -2.0*t**2*sin(s)**2 + 1.0*t**2 + 1.0*t*sin(s) + 1.0*sin(s)
    #   f_omega = (1.0*t + 1)*sin(s)
    #   f_v     = (1.0*t + 1)*sin(s)
    #   f_phi   = (1.0*t + 1)*sin(s)

    [problem]
    name = "MMS_fully_coupled_chi_const"
    neq = 4
    problem_type = "ooc"

    [time_parameters]
    T  = 1.0
    dt = 0.025

    [physical_parameters.viscosity]
    nu      = 1.0
    mu      = 1.0
    epsilon = 1.0
    sigma   = 1.0

    [physical_parameters.reaction]
    a = 1.0
    c = 1.0

    [physical_parameters.coupling]
    b = 1.0
    d = 1.0

    [physical_parameters.chemotaxis]
    type = "constant"
    k1   = 1.0
    k2   = 1.0

    [physical_parameters.tumor_suppression]
    m1 = 0.0
    m2 = 1.0

    [discretization]
    n_elements = 40
    tau = [0.5, 0.5, 0.5, 0.5]
    gam = 1.0   # Upwinding parameter for static condensation (0.0 = no upwind)

    [initial_conditions]
    u     = "zeros"
    omega = "zeros"
    v     = "zeros"
    phi   = "zeros"

    [force_functions]
    u     = "-2.0*t**2*sin(s)**2 + 1.0*t**2 + 1.0*t*sin(s) + 1.0*sin(s)"
    omega = "(1.0*t + 1)*sin(s)"
    v     = "(1.0*t + 1)*sin(s)"
    phi   = "(1.0*t + 1)*sin(s)"

    [exact_solutions]
    u     = "t*sin(s)"
    omega = "t*sin(s)"
    v     = "t*sin(s)"
    phi   = "t*sin(s)"

    [exact_solution_derivatives]
    u     = "t*cos(s)"
    omega = "t*cos(s)"
    v     = "t*cos(s)"
    phi   = "t*cos(s)"

    [boundary_conditions]
    # Dirichlet at B0 (left, s=0), Neumann at B1 (right).
    # Neumann data = outward normal flux = +flux at right.
    B0_u = { type = "dirichlet", data = "t*sin(s)" }
B1_u = { type = "neumann",   data = "1.0*t*(-t*sin(s) + 1)*cos(s)" }
B0_omega = { type = "dirichlet", data = "t*sin(s)" }
B1_omega = { type = "neumann",   data = "1.0*t*cos(s)" }
B0_v = { type = "dirichlet", data = "t*sin(s)" }
B1_v = { type = "neumann",   data = "1.0*t*cos(s)" }
B0_phi = { type = "dirichlet", data = "t*sin(s)" }
B1_phi = { type = "neumann",   data = "1.0*t*cos(s)" }
