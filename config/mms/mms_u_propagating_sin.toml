    # MMS test: u_propagating_sin
    # Auto-generated by validation_scripts/mms_ooc_upwind.py
    #
    # Manufactured solutions:
    #   u     = t*sin(s + 2*t)
    #   omega = 0
    #   v     = 0
    #   phi   = 0
    #
    # Forcing terms:
    #   f_u     = 1.0*t*sin(s + 2*t) + 2*t*cos(s + 2*t) + sin(s + 2*t)
    #   f_omega = 0
    #   f_v     = 0
    #   f_phi   = 0

    [problem]
    name = "MMS_u_propagating_sin"
    neq = 4
    problem_type = "ooc"

    [time_parameters]
    T  = 1.0
    dt = 0.025

    [physical_parameters.viscosity]
    nu      = 1.0
    mu      = 1.0
    epsilon = 1.0
    sigma   = 1.0

    [physical_parameters.reaction]
    a = 1.0
    c = 1.0

    [physical_parameters.coupling]
    b = 0.0
    d = 0.0

    [physical_parameters.chemotaxis]
    type = "constant"
    k1   = 0.0
    k2   = 1.0

    [physical_parameters.tumor_suppression]
    m1 = 0.0
    m2 = 1.0

    [discretization]
    n_elements = 40
    tau = [0.5, 0.5, 0.5, 0.5]

    [initial_conditions]
    u     = "zeros"
    omega = "zeros"
    v     = "zeros"
    phi   = "zeros"

    [force_functions]
    u     = "1.0*t*sin(s + 2*t) + 2*t*cos(s + 2*t) + sin(s + 2*t)"
    omega = "zeros"
    v     = "zeros"
    phi   = "zeros"

    [exact_solutions]
    u     = "t*sin(s + 2*t)"
    omega = "zeros"
    v     = "zeros"
    phi   = "zeros"

    [exact_solution_derivatives]
    u     = "t*cos(s + 2*t)"
    omega = "zeros"
    v     = "zeros"
    phi   = "zeros"

    [boundary_conditions]
    # Dirichlet at B0 (left, s=0), Neumann at B1 (right).
    # Neumann data = outward normal flux = +flux at right.
    B0_u = { type = "dirichlet", data = "t*sin(s + 2*t)" }
B1_u = { type = "neumann",   data = "1.0*t*cos(s + 2*t)" }
# omega: exact solution zero — homogeneous Neumann by default
# v: exact solution zero — homogeneous Neumann by default
# phi: exact solution zero — homogeneous Neumann by default
